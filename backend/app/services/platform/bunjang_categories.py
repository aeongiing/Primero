"""[여원] 번개장터 카테고리 ID 매핑.

AI 매핑 결과(예: "여성의류 > 상의 > 니트/스웨터")를 번개 API 의 categoryId 로 변환.
ID 는 Network 캡처에서 확인한 값 기반. 없으면 기본값(여성의류>상의>반팔)을 쓴다.

⚠️ 비공식 ID라 번개 내부 변경 시 깨질 수 있음.
"""

BUNJANG_CATEGORY_IDS: dict[str, str] = {
    # 캡처로 확인된 값
    "여성의류 > 상의 > 니트/스웨터": "900500100",
    # 아래는 패턴 추정값(실제 등록 시 검증 필요)
    "여성의류 > 아우터 > 코트": "900100300",
    "여성의류 > 아우터 > 자켓": "900100400",
    "여성의류 > 상의 > 반팔 티셔츠": "900500600",
    "여성의류 > 바지 > 데님/청바지": "900200100",
    "여성의류 > 원피스 > 미디 원피스": "900400200",
    "남성의류 > 상의 > 맨투맨": "910500200",
    "남성의류 > 상의 > 니트/스웨터": "910500300",
    "남성의류 > 아우터 > 자켓": "910100400",
    "남성의류 > 바지 > 데님/청바지": "910200100",
}

_DEFAULT_CATEGORY_ID = "900500100"  # 여성의류 > 상의 > 니트/스웨터


def get_bunjang_category_id(category_path: str) -> str:
    """카테고리 경로 → 번개 categoryId. 없으면 기본값."""
    return BUNJANG_CATEGORY_IDS.get(category_path, _DEFAULT_CATEGORY_ID)
